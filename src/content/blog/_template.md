---
# Files starting with "_" are ignored by the site.
# To publish a post: copy this file, rename it (the filename becomes the URL slug,
# e.g. "grpo-collapse-modes.md" -> /writing/grpo-collapse-modes), fill in the
# frontmatter, and set draft: false.
title: 'Post title'
description: 'One-sentence summary shown in lists and RSS.'
date: 2026-07-19
draft: true
---

Write markdown here.

Math works inline like $\pi(a\mid s)$ and in blocks:

$$
J(\theta) = \mathbb{E}_{\tau \sim \pi_\theta}\left[\sum_t r_t\right]
$$

Code is highlighted:

```python
def hello():
    return "world"
```
